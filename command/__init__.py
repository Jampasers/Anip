"""Command package initialization module.

This file marks the `command` directory as a Python package. It doesn't need
to contain any code, but it's required so that Python can import modules
from this directory using the `command.<module>` notation.
"""
